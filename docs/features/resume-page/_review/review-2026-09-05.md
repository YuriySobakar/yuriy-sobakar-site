---
feature: resume-page
date: 2026-09-05
feature_size: S
route: quick
reviewer: sdd:reviewer (clean context, model sonnet, effort high — per .claude/sdd.local.md)
scope: a0a3089...feat/resume-page (12 commits, T1–T11 + 1 placeholder fix)
verdict: PASS
---

# Review — resume-page — 2026-09-05

## Scope

- Base: `a0a3089` (branch point від `main`), гілка `feat/resume-page`, 12 комітів до рев'ю.
- Diff stat: 28 файлів, +1162 / −114 (`lib/resume/{rules,code-hosts,view-model,load}.js`,
  `src/_data/resume.schema.json`, `src/_includes/**`, `src/styles/*`, `eleventy.config.js`,
  `test/*.test.js` + 3 fixtures, `CLAUDE.md`, `docs/architecture-map.md`, `tasks/tracker.md`).
- Заявлені AC (SDD-AC трейлери + `tasks.json`): AC-01, AC-02, AC-03, AC-03b, AC-04, AC-05, AC-06,
  AC-07, AC-08, AC-09, AC-10, AC-11 — повний набір §5.
- Гейт на момент рев'ю: `npm test` 39/39 pass; `npm run build` OK; `npm run lint` (html-validate) exit 0.
- Артефакти без даних/API (легальний пропуск для статичного сайту): немає `data-model.md` та
  `contracts/openapi.yaml`; контракт даних — `src/_data/resume.schema.json` + `lib/resume/rules.js`.

## Stage 1 — AC compliance + наскрізна трасировка

**Знахідок немає.** Кожна §4 user story має ≥1 AC та §6 flow у `sad.md`; кожен §5 AC доходить до
коду і тесту, UI-AC — до `ui`-таски з SCR-id та build-тесту по зібраному HTML
(`test/build.test.js` на fixtures `resume.full.yaml` / `resume.empty-sections.yaml` /
`resume.invalid-rule.yaml`), а не лише до юніт-тесту view model. ADR 0001 (schema + rules як
publish gate), ADR 0002 (шаблони рендерять лише санітизований view model), ADR 0003
(cookie-less лічильник, вимкнений у `site.json` до появи акаунта) — дотримані.

Свідомі розриви, узгоджені самою spec (§6 NFR «Адаптивність», «Доступність»: «ручна перевірка
перед публікацією», Lighthouse при кожному релізі) — **не знахідки**, а manual DoD для `ship`:

| Пункт | Що перевірити вручну на deploy preview |
|---|---|
| US-07 / AC-10 | 360 / 768 / 1280 px без горизонтальної прокрутки |
| AC-01 (частина «до прокрутки») | hero видно на 360×640 без скролу |
| NFR a11y / вага | Lighthouse Accessibility ≥ 95; transferred ≤ 150 KB |

### Trace table

| US/AC | §6 flow | screens.md | Task | Код | Тест | Статус |
|---|---|---|---|---|---|---|
| US-01 | Flow 3, 4 | SCR-02, SCR-06 default | T5, T9 | `hero.njk`, `contacts.njk` | `build.test.js` landmarks/facts/contacts | OK |
| US-02 | Flow 2, 3 | SCR-03 default / current | T2, T6 | `view-model.js`, `experience.njk` | `view-model.test.js`, `build.test.js` newest-first | OK |
| US-03 | Flow 2, 3 | SCR-04, SCR-05 | T2, T7, T8 | `view-model.js`, `skills.njk`, `projects.njk`, `code-hosts.js` | `view-model.test.js`, `build.test.js` | OK |
| US-04 | Flow 1, 2 | усі SCR default·updated | T1–T11 | schema / view-model / templates | `build.test.js` verbatim marker; QG-3 — manual | OK |
| US-05 | Flow 1, 2, 3 | error/empty SCR-02…05 | T1, T2, T3 | `rules.js`, `load.js`, `view-model.js` | `resume-rules`, `resume-schema`, `build` (empty, invalid-rule) | OK |
| US-06 | Flow 2, 3 | SCR-05 confidential | T2, T8 | `view-model.js`, `projects.njk` | `view-model.test.js`, `build.test.js` AC-06 leak scan | OK |
| US-07 | Flow 3 | SCR-01 @360/768/1280 | T4 | `layout.css`, `tokens.css` | manual (spec §6) | manual |
| AC-01 | Flow 3 | SCR-02 default | T5 | `hero.njk` | `build.test.js` | OK |
| AC-02 | Flow 4 | SCR-02 / SCR-06 tap | T5, T9 | `hero.njk`, `contacts.njk`, `layout.njk` | `build.test.js` contacts + counter | OK |
| AC-03 | Flow 2 else | SCR-03 default | T2, T6 | `view-model.js`, `experience.njk` | `view-model.test.js`, `build.test.js` | OK |
| AC-03b | Flow 2 alt | SCR-03 error (stderr) | T1, T3 | `rules.js`, `load.js` | `resume-rules.test.js`, `resume-schema.test.js` | OK |
| AC-04 | Flow 2 else | SCR-04 default | T2, T7 | `view-model.js`, `skills.njk` | `view-model.test.js`, `build.test.js` | OK |
| AC-05 | Flow 2 alt | SCR-05 error | T1, T8 | `rules.js`, `code-hosts.js`, `projects.njk` | `resume-rules.test.js`, `build.test.js` invalid-rule | OK |
| AC-06 | Flow 2 else / Flow 3 | SCR-05 confidential | T2, T8 | `view-model.js`, `projects.njk` | `view-model.test.js`, `build.test.js` | OK |
| AC-07 | Flow 2 alt | SCR-02/05 error | T1, T3 | `resume.schema.json`, `rules.js`, `load.js` | `resume-rules.test.js` (1–3 facts), `resume-schema.test.js` | OK |
| AC-08 | Flow 2 else | SCR-03/04/05 empty | T2, T4, T7 | `view-model.js`, `index.njk` | `view-model.test.js`, `build.test.js` empty-sections | OK |
| AC-09 | Flow 1 alt / Flow 3 alt | SCR-01 previous version | T3 | `load.js` | `build.test.js` invalid-rule → `indexWritten:false` | OK |
| AC-10 | Flow 3 | SCR-01 @360/768/1280 | T4 | `layout.css`, `tokens.css` | manual (spec §6) | manual |
| AC-11 | Flow 1 else / Flow 2 | SCR-01 default·updated | T3, T10, T11 | `load.js`, docs | `build.test.js` verbatim marker | OK |

## Stage 2 — якість

### F1 — `confidential` не обмежений на `kind: commercial` — **Fix now → виправлено**

- Цитати: `src/_data/resume.schema.json:89-90` (`confidential` — незалежний boolean, без `if/then`),
  `lib/resume/rules.js:41` (ранній `return` для не-commercial пропускав перевірку),
  `lib/resume/view-model.js:25-33` (`presentProject` стирає `name`/`links`/`stack` у будь-якого
  `confidential`, незалежно від `kind`), `CONTEXT.md:13-14` (глосарій: confidential project — це
  commercial project).
- Проблема: `confidential: true` на pet-проєкті проходив publish gate, а на сайт мовчки потрапляла
  картка з порожнім `<h3>` (`projects.njk:10-11`). Жоден AC не порушений — домен-інваріант із
  глосарію, саме той клас правил, для якого ADR 0001 завів `rules.js`.
- Рішення (власник, 2026-09-05): **Fix now**. Комміт `dcad8ca` на `feat/resume-page`:
  правило `RULES.confidentialPet` у `lib/resume/rules.js` + тест «confidential on a pet project is
  one violation» у `test/resume-rules.test.js` (RED: actual 0 / expected 1 → GREEN).
- Гейт після фіксу: `npm test` 40/40; `npm run build` OK; `npm run lint` exit 0.

Інших stage-2 знахідок немає: межі модулів (`lib/resume` ↔ шаблони), tokens-only CSS, `safe`-фільтр
відсутній, XSS-поверхні немає, `print.css` не зачеплено, `dir.input`/`dir.output` не повертаються з
`eleventy.config.js`, тести не послаблені; `CLAUDE.md` і `docs/architecture-map.md` звірені з кодом.

### Нотатка (не знахідка): `docs/design-system.md` відсутній

Примітив `fact-list` (T5) зареєстрований у `docs/architecture-map.md` §shared primitives; канон
`docs/design-system.md` у репозиторії ще не заведений (`/sdd:design-system` не запускався — фіча S,
код-режим). Реєстрація туди — коли/якщо канон з'явиться; на ship не впливає.

## Gate result

**PASS** — stage 1 чистий, єдина stage-2 знахідка виправлена й закомічена (`dcad8ca`). Manual DoD
(адаптивність 360/768/1280, hero above the fold, Lighthouse) переходять у `ship` на deploy preview.
